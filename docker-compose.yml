services:
  backend:
    build:
      context: ./backend
    container_name: employee-backend-compose
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend
    container_name: employee-frontend-compose
    ports:
      - "5173:5173"
